{
  "name": "frontend",
  "version": "1.0.0",
  "description": "frontend for sdcaulley",
  "main": "index.html",
  "scripts": {
    "start": "es-dev-server --node-resolve --watch --app-index index.html --open"
  },
  "author": "sdcaulley",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "happiness": "^10.0.2"
  },
  "dependencies": {
    "@vaadin/router": "^1.7.2",
    "lit-element": "^2.3.1",
    "normalize.css": "^8.0.1"
  },
  "standard": {
    "globals": [
      "customElements",
      "fetch",
      "URL",
      "Proxy"
    ]
  },
  "proxy": "localhost:5000"
}
