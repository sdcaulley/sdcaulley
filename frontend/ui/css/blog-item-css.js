import { css } from 'lit-element'

export const placement = css`
  :host {
    position: relative;
    width: 100%;
  }

  h4 {
    margin: 0;
    padding: 0.25em;
  }

  a {
    display: inline-block;
    margin: 1em;
  }

  .tags {
    display: inline-block;
  }
`
